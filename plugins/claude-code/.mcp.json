{
  "mcpServers": {
    "opentrace-oss": {
      "type": "stdio",
      "command": "uvx",
      "args": [
        "opentraceai",
        "mcp"
      ],
      "description": "OpenTrace knowledge graph tools (queries a local indexed codebase)."
    }
  }
}

// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v3.21.12
// source: opentrace/v1/code_graph.proto

/* eslint-disable */

export const protobufPackage = "opentrace.v1";

/** RepositoryNode represents a source code repository. */
export interface RepositoryNode {
  id: string;
  name: string;
  ref: string;
  sourceUri: string;
  provider: string;
  defaultBranch: string;
  owner: string;
  summary: string;
}

/** DirectoryNode represents a directory in a repository. */
export interface DirectoryNode {
  id: string;
  name: string;
  path: string;
  summary: string;
}

/**
 * ModuleNode represents a language-level module or package.
 * Examples: Python module/package, Java package, Go package, Rust crate/module,
 * Node.js package, C# namespace/assembly.
 */
export interface ModuleNode {
  id: string;
  name: string;
  language: string;
  /** filesystem path or qualified name (e.g. "com.example.auth") */
  path: string;
  /** "module", "package", "crate", "namespace", "assembly" */
  kind: string;
  /** for external dependencies */
  version: string;
  docs: string;
  exported: boolean;
}

/** FileNode represents a source code file. */
export interface FileNode {
  id: string;
  name: string;
  path: string;
  extension: string;
  language: string;
  sourceUri: string;
  /** total line count */
  lines: number;
  summary: string;
}

/**
 * ClassNode represents a class, struct, record, type alias, or similar type definition.
 * For type aliases (e.g. TS `type Props = { ... }`, Go `type Handler func(...)`),
 * use kind="type_alias". For anonymous types, add "anonymous" to modifiers.
 */
export interface ClassNode {
  id: string;
  name: string;
  language: string;
  startLine: number;
  endLine: number;
  signature: string;
  docs: string;
  exported: boolean;
  /** "class", "struct", "record", "dataclass", "union", "type_alias" */
  kind: string;
  /** "public", "abstract", "sealed", "final", "static", "anonymous", etc. */
  modifiers: string[];
  superclasses: string[];
  interfaces: string[];
  /** generics: ["T", "U extends Foo"] */
  typeParameters: string[];
  /** @dataclass, @Entity, @Component, #[derive(...)], etc. */
  decorators: string[];
  /** AI-generated summary */
  summary: string;
}

/**
 * EnumNode represents an enum type definition.
 * The enum itself is a node; its variants/members are represented as
 * Variable nodes with kind="enum_variant", connected via Defines edges.
 */
export interface EnumNode {
  id: string;
  name: string;
  language: string;
  startLine: number;
  endLine: number;
  docs: string;
  exported: boolean;
  /** "enum", "flags", "adt" (Rust algebraic), "string_enum" (TS) */
  kind: string;
  modifiers: string[];
  /** Rust: enum Option<T> */
  typeParameters: string[];
  decorators: string[];
  summary: string;
}

/** InterfaceNode represents an interface, trait, or protocol definition. */
export interface InterfaceNode {
  id: string;
  name: string;
  language: string;
  startLine: number;
  endLine: number;
  signature: string;
  docs: string;
  exported: boolean;
  /** "interface", "trait", "protocol", "abstract_class" */
  kind: string;
  /** "public", "sealed", "partial", etc. */
  modifiers: string[];
  typeParameters: string[];
  decorators: string[];
  summary: string;
}

/** MethodNode represents a method defined on a class or interface. */
export interface MethodNode {
  id: string;
  name: string;
  language: string;
  startLine: number;
  endLine: number;
  signature: string;
  docs: string;
  exported: boolean;
  returnType: string;
  /** "public", "private", "static", "abstract", "async", "virtual", "override", etc. */
  modifiers: string[];
  typeParameters: string[];
  /** @override, @staticmethod, @property, etc. */
  decorators: string[];
  /** parameter names: ["ctx", "name", "options"] */
  parameters: string[];
  /** declared exceptions: Java throws, Kotlin @Throws, Swift throws */
  throws: string[];
  summary: string;
}

/** FunctionNode represents a standalone function (not bound to a class). */
export interface FunctionNode {
  id: string;
  name: string;
  language: string;
  startLine: number;
  endLine: number;
  signature: string;
  docs: string;
  exported: boolean;
  returnType: string;
  /** "async", "export", "inline", "unsafe", "extern", etc. */
  modifiers: string[];
  typeParameters: string[];
  /** @app.route(), @click.command(), #[test], etc. */
  decorators: string[];
  /** parameter names: ["req", "res"] */
  parameters: string[];
  /** declared exceptions */
  throws: string[];
  summary: string;
}

/**
 * DependencyNode represents an external package dependency declared in a manifest
 * (package.json, pyproject.toml, go.mod, pom.xml, Cargo.toml, Gemfile, etc.).
 * Linked from Module/Repo via DependsOn edges.
 */
export interface DependencyNode {
  id: string;
  name: string;
  /** version constraint: "^4.17", ">=2.28", "v1.2.3" */
  version: string;
  /** "npm", "pypi", "go", "maven", "crates.io", "rubygems" */
  registry: string;
  /** "runtime", "dev", "peer", "optional", "build" */
  kind: string;
  /** URL to package registry page */
  sourceUri: string;
}

/** VariableNode represents a module-level variable, constant, field, or export. */
export interface VariableNode {
  id: string;
  name: string;
  language: string;
  startLine: number;
  endLine: number;
  /** "const", "let", "var", "field", "property", "parameter" */
  kind: string;
  exported: boolean;
  /** declared type if present */
  typeAnnotation: string;
  docs: string;
  /** "public", "private", "static", "readonly", "volatile", "final", "lazy", etc. */
  modifiers: string[];
  /** @property, @Value, #[serde(...)], etc. */
  decorators: string[];
}

/** PullRequestNode represents a pull request / merge request. */
export interface PullRequestNode {
  id: string;
  name: string;
  number: number;
  title: string;
  /** "open", "closed", "merged" */
  state: string;
  author: string;
  url: string;
  createdAt: string;
  baseBranch: string;
  headBranch: string;
  additions: number;
  deletions: number;
  filesChanged: number;
}

/**
 * DefinesRel represents a structural containment relationship:
 * Repo defines Dir, Dir defines File, File defines Class, etc.
 */
export interface DefinesRel {
  id: string;
  sourceId: string;
  targetId: string;
}

/**
 * ImportsRel represents an import/use/require dependency.
 * Source is the logical consumer (Module, Class); target is what it depends on.
 * Examples:
 *   Module --Imports--> Module    (Python: import os, Go: import "fmt")
 *   Module --Imports--> Class     (Python: from collections import OrderedDict)
 *   Module --Imports--> Function  (Python: from os.path import join)
 *   Class  --Imports--> Class     (Java: import com.example.Foo)
 */
export interface ImportsRel {
  id: string;
  sourceId: string;
  targetId: string;
  /** import alias if renamed (e.g. "import numpy as np" → alias="np") */
  alias: string;
}

/** CallsRel represents a function/method call relationship. */
export interface CallsRel {
  id: string;
  sourceId: string;
  targetId: string;
  confidence: number;
}

/** ImplementsRel represents a class implementing an interface. */
export interface ImplementsRel {
  id: string;
  sourceId: string;
  targetId: string;
}

/** ExtendsRel represents class inheritance. */
export interface ExtendsRel {
  id: string;
  sourceId: string;
  targetId: string;
}

/**
 * OverridesRel represents a method overriding a parent class/interface method.
 * Method --Overrides--> Method
 */
export interface OverridesRel {
  id: string;
  sourceId: string;
  targetId: string;
}

/**
 * DependsRel represents a build-level package dependency.
 * Module/Repo --DEPENDS--> Dependency
 */
export interface DependsRel {
  id: string;
  sourceId: string;
  targetId: string;
}

/**
 * ChangesRel represents a pull request modifying a file.
 * PullRequest --Changes--> File
 */
export interface ChangesRel {
  id: string;
  sourceId: string;
  targetId: string;
  additions: number;
  deletions: number;
  /** "added", "modified", "removed", "renamed" */
  status: string;
}

/**
 * ReferencesRel represents a read/use of a variable, constant, or type
 * without invoking it (distinct from Calls).
 * Function/Method --References--> Variable/Class/Enum
 */
export interface ReferencesRel {
  id: string;
  sourceId: string;
  targetId: string;
}

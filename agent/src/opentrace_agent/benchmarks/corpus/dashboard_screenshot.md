# Image description (latency-dashboard.png)

A screenshot of a Grafana dashboard titled "Search Indexer — p99
Latency". The main panel is a time-series chart showing p99 latency for
the search indexer over the past 7 days, with a visible spike on
Tuesday reaching 2.4s against a baseline near 300ms.

A legend below the chart reads "source: prometheus". Navigation chrome
is visible: a left sidebar with menu items "Dashboards", "Explore",
"Alerting", a time-range picker set to "Last 7 days", and buttons
labelled "Refresh" and "Share".

The chart Y-axis is labelled "seconds" and the X-axis shows dates.

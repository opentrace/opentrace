# Image description (architecture-diagram.png)

A hand-drawn architecture diagram on a whiteboard, photographed at an
angle. Three labelled boxes are connected by arrows.

The box labelled "Order API" has an arrow pointing to the box labelled
"Billing Worker". The arrow is annotated "publishes to" above a smaller
label reading "queue". The "Billing Worker" box has an arrow to a
cylinder shape labelled "Ledger DB", annotated "writes".

A fourth, partially erased box in the corner may read "Audit Log"; a
faint arrow from "Billing Worker" appears to point at it but the
annotation is illegible.

Colours: boxes outlined in blue marker, arrows in black. A sticky note
in the top-right corner reads "v2 — do not erase".

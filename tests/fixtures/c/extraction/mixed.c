struct Node {
    int value;
    struct Node* next;
};

enum Direction {
    NORTH,
    SOUTH,
    EAST,
    WEST
};

struct Node* create_node(int val) {
    return 0;
}

void free_node(struct Node* n) {
}

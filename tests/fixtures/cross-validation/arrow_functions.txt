const greet = (name: string): string => {
  return `Hi ${name}`;
};
const double = (n: number) => n * 2;
export const handler = async (req: Request) => {
  return Response.json({});
};
const process = function(data: string) {
  return transform(data);
};
